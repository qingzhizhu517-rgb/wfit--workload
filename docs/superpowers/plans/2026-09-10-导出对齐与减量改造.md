# 导出对齐与减量改造实现计划

- 日期：2026-09-10
- 工程：`C:\Users\Aohs\Desktop\wfit\wfit--workload`（rear/ Spring Boot 4.0.7 + JDK17 包名 `com.workload` 端口 8084；front/ RuoYi-Vue3）
- 交付主体：**本工程（A）**。B（`wfit-workload_副本`）仅为参考，其可吸收项见 Phase 8（**待你勾选，本计划暂不纳入**）
- 依据：
  - `else/附件1：教师教育教学工作量统计表-新.xlsx` —— **39 列（A~AM），导出基准（已确认）**
  - `else/附件2：教师教育教学工作量绩效酬金统计表-新.xlsx` —— 10 列主表 + 15 列其他酬金明细
  - `else/潍坊理工学院教师工作量管理办法(征集意见稿）.pdf` —— 条文依据（已提取全文）
  - `Downloads\测试数据-教师工作量统计.xlsx` —— **仅「工作量统计」sheet（表一）作参考**（已确认）；其旧版 37 列编号体系不采用
  - `docs/导出对齐与减量实施方案-2026-09-10.md` —— 本计划的分析前置
- 执行方式：**先规划评审 → 批准后开工**；建议 subagent-driven，每个 Task 独立提交

---

## Context

系统 M1–M5 已完成，能跑通「导入 → 核算 → 汇总 → 酬金 → 导出」全流程。但本轮核查发现三类问题：

1. **导出与交付物不同构**。现在的附件1 是 19 列「一行一环节」长表，而学校要的附件1 是 39 列「一行一开课任务」宽表——**行粒度都不同**，不是改列名能解决的。
2. **数据库缺交付必需字段**。最硬的一处：附件2 需要**身份证号**，而 `biz_teacher_profile` 没有这个字段。
3. **若干口径与《办法》原文不符**。逐条对读后确认了 5 处可据条文修正（G1 多乘 Q3、G4/G5 截断无依据、C1 分组键多一项、其他酬金 A 项 ≥20 人语义），另有 1 处（200% 封顶）条文本身歧义，须教务处回签。

同时按「减量 AI 实现场景」，审批流简化为两级、剔除院领导签字等非必要内容。

**现状关键事实（已核实）**：

- 提交状态：`45c4fe1` 已固化 E/F/G 三轮修复，工作区干净
- 运行态：MySQL 3306 / Redis 6379 / 后端 8084 均在跑；jar 构建于 09-01 17:40（含全部修复）
- `mvn` **不在 bash PATH**——需用 IDEA 或完整路径；无 `mvnw`
- 汇总数据现状：2 条汇总（`user_id=2003` 钱伟基线 `G7=947.33 / 总=982.33 / 超额=802.33`；`user_id=2008` 全 0），均为 `status=0`
- 测试基础薄弱：仅 `CalcStrategyFactoryTest` / `StrategyCacheTest` 两个测试类，**无集成测试**
- `biz_workload_rule` 39 条，`RuleParamServiceImpl` 写 Redis **不设 TTL**——**Phase 6 之前改规则必须手工删 `wl_rule:<CODE>` 键**
- 审批相关字段齐备：`teacher_sign` / `academic_assistant_sign` / `dept_leader_sign` + 三个时间 + `lock_time`

---

## 已定决策（本轮输入，不再讨论）

| # | 决策 |
|---|---|
| 1 | 导出基准 = **39 列「-新」模板** |
| 2 | 参考件**只有表一**；其表二不作参考 |
| 3 | **专升本归本科**；**重修另设标志**（不放进层次列） |
| 4 | 审批流简化为 `0 填报中 → 1 教务处待审 → 2 已完结`，`-1 驳回`；admin 保留超管 |
| 5 | 移除 3 个模块、8 个明细页合并为 1、去掉 Redis 依赖 |
| 6 | 经费口径**一并回签处理**（Phase 5） |
| 7 | 先固化提交（已完成），**规划批准后**再改代码 |

---

## 目标状态

```
结构    rear: workload-admin / workload-system / workload-framework / workload-common（4 模块）
        审批: 0 填报中 → 1 教务处待审 → 2 已完结(锁定)；-1 驳回
数据    biz_teacher_profile + 身份证三列（密文/掩码/HMAC）
        biz_workload_summary + G7 列 + 两个说明列
交付    附件1 = 39 列宽表（A~AM，含固定行与签字区）
        附件2 = 10 列主表 + 15 列其他酬金明细（含身份证号）
口径    G1 不含 Q3；C1 分组键 = 教师+学期+课程名+层次；G4/G5 不截断仅告警；
        G6 截断 20（有依据）；其他酬金 A 项 ≥20 归零+告警；代阅卷 D 已启用
界面    工作量明细 1 页（替代 8 页）；仪表盘 2 个（教师 / 教务处）
依赖    不依赖 Redis
```

---

## 阶段与 Task 拆分

> 顺序有依赖：**Phase 2（补字段）与 Phase 3（口径）必须先于 Phase 4（导出）**，因为导出要取新字段与新数值。
> Phase 1/5/6 相对独立，可并行。

### Phase 1：结构性减量（不动算法）

#### Task 1：移除三个未使用模块

**Files:**
- Delete: `rear/manage/`
- Delete: `rear/workload-generator/`
- Delete: `rear/workload-quartz/`
- Modify: `rear/pom.xml`（`<modules>` 去掉三项）
- Modify: `rear/workload-admin/pom.xml`（移除对被删模块的依赖，若有）
- Modify: `CLAUDE.md` / `PROJECT_CONTEXT.md` / `AGENTS.md`（模块清单与目录树同步）

**Steps:**
1. 先确认无引用：`grep -rn "workload-quartz\|workload-generator" rear --include=pom.xml --include=*.java`
   - 预期：仅 `rear/pom.xml` 的 `<modules>` 与 `CLAUDE.md` 描述命中
2. 删除目录 + 改 `rear/pom.xml`
3. 全量编译验证：IDEA 里 `mvn clean package -DskipTests`（或完整路径 mvn）
   - 预期：BUILD SUCCESS；若报找不到 quartz 相关类，说明有隐藏引用，需回退该模块
4. 提交：`chore: 移除未使用的 manage / generator / quartz 模块`

> ⚠ `quartz.sql` 保持不动（基础表已在库里），仅移除代码模块。

#### Task 2：审批流简化为两级

**Files:**
- Modify: `rear/workload-system/src/main/java/com/workload/system/service/impl/BizAuditServiceImpl.java`
- Modify: `rear/workload-system/src/main/java/com/workload/system/controller/BizAuditController.java`
- Modify: `rear/workload-system/src/main/java/com/workload/system/service/impl/BizWorkloadSummaryServiceImpl.java`
- Modify: `rear/sql/13_fix_audit_perm.sql`（或新增 `16_simplify_approval.sql`）
- Modify: 前端 `front/RuoYi-Vue3/src/views/system/workloadSummary/index.vue`

**Steps:**
1. **先定状态语义**，写进代码注释：
   ```
   0 填报中 → 1 教务处待审 → 2 已完结(锁定)；-1 驳回 → 回 0
   ```
2. `BizAuditServiceImpl`：
   - `approve()`：由「转 status=2 待签」改为「**转 status=2 已完结 + 置 `lock_time`**」
   - `sign()`：**删除该方法**（及其 Controller 端点 `POST /system/audit/sign`）
   - `reject()`：`from` 由 `{1,2}` 收窄为 `{1}`
   - 保留 `rejectSummary` 中清空三个签字字段的逻辑（防回归），但写入方只剩 `teacher_sign` 与 `academic_assistant_sign`
3. `BizWorkloadSummaryServiceImpl`：所有「`status==3` 视为已完结」的判定改为 `status==2`
   - 全文检索 `status == 3` / `setStatus(3)` / `getStatus() == 3` 逐处确认
4. **数据迁移脚本** `rear/sql/16_simplify_approval.sql`（幂等）：
   ```sql
   UPDATE biz_workload_summary SET status = 2 WHERE status = 3;
   -- 回收院领导签字权限
   DELETE FROM sys_role_menu WHERE menu_id IN
     (SELECT menu_id FROM sys_menu WHERE perms = 'system:audit:sign');
   ```
5. 前端：`workloadSummary/index.vue` 去掉「院领导签字」按钮与相关列；状态文案改为「已完结」
6. 执行迁移脚本，并用 `admin_test` 走一遍：填报 → 提交 → 教务助理通过 → 确认锁定
7. 提交：`refactor(audit): 审批流简化为教师+教务处两级`

> ⚠ `dept_leader_sign` / `dept_leader_sign_time` 两列**保留不删**（便于将来回退），仅代码不再写入。

#### Task 3：移除 LeaderDashboard

**Files:**
- Delete: `front/RuoYi-Vue3/src/views/dashboard/LeaderDashboard.vue`
- Modify: `front/RuoYi-Vue3/src/router/index.js`（去掉该路由）
- Modify: `front/RuoYi-Vue3/src/composable/useDashboard.js`（去掉 leader 分支）
- Modify: `rear/sql/05_biz_menu.sql` / `15_fix_menu_buttons.sql`（如含 leader 仪表盘菜单）

**Steps:** 删除 → 检索 `LeaderDashboard` 全文无残留引用 → `npm run build:prod` 通过 → 提交
`chore(ui): 移除院领导工作台`

---

### Phase 2：数据库补字段

#### Task 4：身份证号三列（加密）

**Files:**
- Create: `rear/sql/17_teacher_id_card.sql`
- Modify: `rear/workload-system/src/main/java/com/workload/system/domain/BizTeacherProfile.java`
- Create: `rear/workload-common/src/main/java/com/workload/common/utils/CryptoUtil.java`（AES-GCM）
- Modify: `rear/workload-admin/src/main/resources/application.yml`（密钥占位）
- Modify: `.env.example`（新增两个密钥变量）

**Steps:**
1. DDL（三列并存）：

   ```sql
   ALTER TABLE biz_teacher_profile
     ADD COLUMN id_card_cipher VARBINARY(256) NULL COMMENT '身份证密文 AES-GCM: keyId(1)+iv(12)+ct+tag(16)',
     ADD COLUMN id_card_mask   VARCHAR(32)    NULL COMMENT '掩码 3707**********1234',
     ADD COLUMN id_card_hmac   CHAR(64)       NULL COMMENT 'HMAC-SHA256(密钥,明文)，仅供导入查重',
     ADD UNIQUE KEY uk_teacher_id_card_hmac (id_card_hmac);
   ```
2. 密钥走环境变量：`WFIT_IDCARD_KEY`（base64 32 字节）、`WFIT_IDCARD_HMAC_KEY`；
   **启动时校验存在且长度正确，否则启动失败**（不静默降级为明文）
3. `CryptoUtil`：`encrypt(plain)` / `decrypt(cipher)` / `hmac(plain)` / `mask(plain)`
4. `BizTeacherProfile` 只暴露 `idCardMask`；**密文明文都不出现在列表/详情接口**
5. 新增明文读取端点（仅管理员 + `purpose` 必填 + 写 `biz_audit_log`）
6. 提交：`feat(teacher): 身份证号加密落库（密文/掩码/HMAC 三列）`

> ⚠ 密钥丢失 = 身份证数据永久不可读。**必须离线备份，且不与数据库备份同处存放。**
> ⚠ 本 Task 完成后，附件2 导出才可能带身份证号（Task 19 依赖本 Task）。

#### Task 5：汇总表补 G7 列与两个说明列

**Files:**
- Create: `rear/sql/18_summary_extra_columns.sql`
- Modify: `BizWorkloadSummary.java`、`BizWorkloadSummaryMapper.xml`
- Modify: `SummaryCalcServiceImpl`

**Steps:**
1. DDL：
   ```sql
   ALTER TABLE biz_workload_summary
     ADD COLUMN G7 DECIMAL(10,2) NULL COMMENT '第一课堂工作量合计 G1~G6',
     ADD COLUMN G8_remark VARCHAR(255) NULL COMMENT '其他工作量说明（附件1 AF 列）',
     ADD COLUMN G11_remark VARCHAR(255) NULL COMMENT '管理服务工作量说明（附件1 AI 列）';
   ```
2. `SummaryCalcServiceImpl` 在汇总时写入 `G7 = ΣG1..G6`（现在只在导出时临时算）
3. 前端「学期汇总」页补两个说明列的编辑入口（教师申报 G8/G11 时填写）
4. **存量回填**：对已有汇总行重算一次（`POST /system/calc/recalcSummary`）
5. 提交：`feat(summary): 补 G7 汇总列与两个说明列`

#### Task 6：G5 文科/理工科归属

**Files:**
- Create: `rear/sql/19_thesis_discipline.sql`
- Modify: `rear/.../calc/strategy/ThesisCalcStrategy.java`
- Modify: `rear/.../mapper/system/BizWlThesisMapper.xml`
- Modify: `rear/.../service/impl/TeachingTaskImportServiceImpl.java`

**Steps:**
1. 给 `biz_wl_thesis` 加 `discipline_category`（SCITECH / LIBERAL_ARTS）：
   ```sql
   ALTER TABLE biz_wl_thesis ADD COLUMN discipline_category VARCHAR(20) NULL
     COMMENT '学科门类，供附件1 X/Y 双列分列；艺术类按文史';
   ```
2. 导入时由 `major_category` 推导写入（讲义 Task 22 已按 专业大类×层次 查 K5，此处把同一判断落库）
3. 存量兼容：`NULL` 时导出按 `K5 ∈ {9,5} → 理工 / {6,4} → 文史` 兜底反推
4. 提交：`feat(thesis): G5 落库学科门类，支撑附件1 文科/理工科双列`

#### Task 7：层次字典——专升本归本科、重修另设标志

**Files:**
- Create: `rear/sql/20_education_level.sql`
- Modify: `TeachingTaskImportDTO` / `TeachingTaskImportServiceImpl`
- Modify: `biz_teaching_task`（加 `is_retake` 标志）

**Steps:**
1. DDL：
   ```sql
   ALTER TABLE biz_teaching_task
     ADD COLUMN is_retake TINYINT(1) NOT NULL DEFAULT 0 COMMENT '重修标志，不占层次列';
   ```
2. 导入归一化规则（在 `validateRow` / 字段映射处）：
   - `专升本` → `education_level = 本科`
   - `重修`（或课程名含「（重修）」）→ `is_retake = 1`，层次仍取真实层次
3. 存量清洗：现有 48 条 `education_level` 只有「本科/专科」，无需迁移；仅补校验规则
4. 前端导入预览显示归一化结果，让教务确认
5. 提交：`feat(import): 专升本归本科 + 重修独立标志`

---

### Phase 3：口径修正（按《办法》原文）

#### Task 8：G1 公式移除 Q3

**Files:**
- Modify: `rear/.../calc/strategy/TheoryCalcStrategy.java:37-38`
- Modify: `CLAUDE.md`（公式表）

**Steps:**
1. 依据：第十四条1 `G1＝J1×C1×K1×Q1×Q2×N`；附件1 模板 L 列同 —— **均不含 Q3**
2. 改计算为 `J1 × C1 × K1 × Q1 × Q2 × N`；Q3 字段**保留但不再参与计算**（界面折叠）
3. **改动前先统计影响面**：
   ```sql
   SELECT COUNT(*) FROM biz_wl_theory WHERE Q3 IS NOT NULL AND Q3 <> 1.0;
   ```
   - 若为 0 → 数值不变，安全
   - 若非 0 → 记录受影响的 item_id，改后逐条比对旧/新值并留档
4. 重新核算钱伟基线，确认仍是 `947.33 / 982.33`
5. 提交：`fix(calc): G1 公式移除 Q3（办法与附件1 模板均不含）`

#### Task 9：C1 分组键移除「工作量类别」

**Files:**
- Modify: `rear/.../mapper/system/BizTeachingTaskMapper.xml`（`countSameCourseTask` 等分组查询）
- Modify: `rear/.../service/impl/TeachingTaskImportServiceImpl.java`

**Steps:**
1. 依据：第十四条1「课程名称一致即为同一门课，不分年级，不以课程代码为准。**本专科分别算**」
2. 分组键由 `教师+学期+课程名+授课层次+工作量类别` 改为 `教师+学期+课程名+授课层次`
3. ⚠ **这会改变存量分组结果**：同名课跨类别时将合并为一组，C1 档位重新分配
4. 改动后必须**整学期重算**并核对受影响教师的工作量变化（建议先跑一次 diff 报告）
5. 提交：`fix(calc): C1 分组键移除工作量类别（办法仅要求课程名+本专科）`

#### Task 10：G4 改为不截断 + 告警

**Files:**
- Modify: `rear/.../calc/strategy/CourseDesignCalcStrategy.java`
- Modify: `rear/sql/`（`CAP_R4_MAX` 改为告警阈值语义，或新增 `WARN_R4_MAX`）

**Steps:**
1. 依据：第十四条4 只说「R4≤60」，**未写「超出部分不计算」**；办法在需要截断时会明写（对比第十四条6注4）
2. 计算改为 `J4 × R4 × 0.4`（不再 `min`）
3. 保留 `CAP_R4_MAX=60` 作为**告警阈值**：`R4 > 60` → 置 `is_over_limit=1` 并在备注写明
4. ⚠ 这是**增加**教师工作量 —— 需在采纳前确认（见「待确认」第 3 项）
5. 提交：`fix(calc): G4 不再截断 R4，改为超 60 告警`

#### Task 11：G5 删除死配置 + 报批告警

**Files:**
- Modify: `rear/.../calc/strategy/ThesisCalcStrategy.java`
- Modify: `rear/sql/`（停用 `CAP_R5_BACHELOR` / `CAP_R5_JUNIOR`）

**Steps:**
1. 依据：第十四条5 本科「**R5≤10 时，按实际人数计算**，当 R5＞8 时，须报院长批准」；专科同理（≤15 / ＞15）
2. `ThesisCalcStrategy` 确认**不做任何截断**（当前已如此），仅置 `is_over_limit`
3. **阈值纠正**：本科报批阈值 = 8（现 `APPROVAL_R5_BACHELOR=8` ✅ 保留并接入）；专科报批阈值 = 15
4. 把 `CAP_R5_BACHELOR` / `CAP_R5_JUNIOR` 两行 `status` 置 0（停用），代码中确认零引用后删除
5. 提交：`fix(calc): 清理 G5 死配置，报批阈值接入办法原文（本8/专15）`

#### Task 12：其他酬金 A 项 ≥20 人归零 + 告警

**Files:**
- Modify: `rear/.../calc/allowance/AllowanceAStrategy.java`
- Modify: `BizAllowanceItemServiceImpl`（告警写入）

**Steps:**
1. 依据：第十五条1(2)「学生人数**大于等于20人单独开班，按照教师基本工作量标准计算**」+ 1(3) 只规定 <6 与 6~19 两档
2. `AllowanceAStrategy`：`count ≥ 20` → **金额 0**，并写一条告警「人数已达单独开班标准，本项不计酬金，工作量见理论课」
3. 保留一个人工特批开关（`ext` 字段标记）以备例外——条文用「原则上」留有空间
4. ⚠ **这会减少教师收入**（A7 修复曾把 ≥20 从 260 改为手工金额）
5. 提交：`fix(allowance): A 项 ≥20 人归零并告警（第十五条1(2)）`

#### Task 13：启用代阅卷 D 档

**Files:**
- Create: `rear/.../calc/allowance/AllowanceDStrategy.java`
- Modify: `AllowanceStrategyFactory`（注册）
- Modify: `rear/sql/`（新增 D 档阶梯规则）

**Steps:**
1. 依据：第十五条5 完整五档：`<20 → 0`；`[20,60) → 30`；`[60,120) → 80`；`[120,200) → 100`；`≥200 → 150`
2. 新增规则：`PAY_D_MARKING` 五档（可复用 `biz_workload_rule` 的 range 语义或新增四行）
3. `AllowanceDStrategy` 实现 `getFeeType()` 返回 D；`AllowanceStrategyFactory` 自动按类型装配（现机制已支持 List 注入）
4. **注意 A 现状**：`BizAllowanceItem.status` 注释写「D 代阅卷默认 0（未启用）」——同步更正
5. 单测：四档边界（19/20、59/60、119/120、199/200）
6. 提交：`feat(allowance): 启用代阅卷 D 档（第十五条5 五档阶梯）`

#### Task 14：管理服务口径统一为「学年值 + 引擎折半」

**Files:**
- Modify: `rear/.../calc/ManagementItemGeneratorImpl.java`
- Modify: `rear/.../domain/BizRoleAssignment.java`（字段注释）
- Modify: `rear/sql/`（教学督导标注为学期值）
- Create: `rear/sql/21_service_rate_unit.sql`（如需）

**Steps:**
1. 依据：第十六条给的是**学年值**（360/180/100/120/90/180每班）+ 注文「均以学期为单位进行核算，每学期最高累计不超过180」；
   第十七条教学督导 **15 学时/学期**（原文已是学期值，**不折半**）
2. **消除现有的注释与代码矛盾**（A-1）：`BizRoleAssignment.allowanceRate` 注释写「学年」而代码按「学期」用
3. 建议方案：约定 `allowance_rate` 存**学年值**，引擎恢复 `÷2`；**教学督导单独标记为学期值不折半**
   - 备选（更稳）：加 `unit` 字段（YEAR/SEMESTER），引擎按 unit 决定，彻底消除歧义
4. ⚠ 若改约定，**存量 `allowance_rate` 需要 ×2 迁移**——先查现有数据：
   ```sql
   SELECT id, role_type, allowance_rate FROM biz_role_assignment;
   ```
5. 提交：`fix(calc): 管理服务岗位标准统一为学年值+引擎折半`

#### Task 15：补三项制度性校验

**Files:**
- Create: `rear/.../calc/rule/ComplianceChecker.java`（或并入 `SummaryCalcServiceImpl`）
- Modify: `SummaryCalcServiceImpl`

**Steps:**
1. **第六条 课程数 ≤ 3**：按「重复课算一门、不含实践类」统计，>3 置告警
2. **第六条 三门理论课视同完成基本教学工作量**：满足则 `basic_teaching_met = 1`（欠额告警降级）
   - ⚠ **当前完全没实现**，会**误判教师欠额**——这是本 Task 最高价值项
3. **第八/九/十条 周学时上限**：用**原始学时** `(J1+J2) ÷ 16`（不带任何系数）比对上限（6/12/16/18），超限置告警
4. 提交：`feat(compliance): 补课程数/三门理论课/周学时三项校验`

#### Task 16：统一「截断与降级必须告警」

**Files:**
- Modify: 全部 7 个策略 + `SummaryCalcServiceImpl`
- Modify: 前端明细页（显示告警）

**Steps:**
1. 原则（借自 B 的经验）：**静默截断 = 教师无声少拿钱**
2. 现状盘点：`CourseDesignCalcStrategy`（Task 10 改）、`ConcentratedInternshipCalcStrategy`（`min(R6,20)`）、`ManagementItemGeneratorImpl`（180 截断）、`SummaryCalcServiceImpl`（540 截断）——逐处确认都置了标记
3. 统一把标记落到 `is_over_limit` 或备注文案，并在附件1「系数说明」列体现
4. 提交：`feat(calc): 截断与降级统一告警`

---

### Phase 4：导出对齐（依赖 Phase 2、3）

#### Task 17：附件1 改为「一行一开课任务」39 列

**Files:**
- Rewrite: `rear/.../domain/dto/PersonalWorkloadDetailExportDTO.java`（39 列）
- Rewrite: `rear/.../controller/BizExportController.java` 的附件1 组装逻辑
- Modify: `rear/.../mapper/system/BizExportMapper.xml`（按开课任务聚合 G1~G6）

**Steps:**
1. DTO 按 A~AM 39 列重写（字段名用语义名，不用 Excel 列标）：

   | 列 | 字段 | 来源 |
   |---|---|---|
   | A | collegeName | sys_dept |
   | B | teacherName | sys_user |
   | C | courseName | biz_workload_item |
   | D | educationLevel | 归一化后（专升本→本科） |
   | E-K | j1,c1,k1,q1,q2,q3,n | `biz_wl_theory` |
   | L | g1 | calculated_workload |
   | M-P | j2,practiceK,c2,g2 | `biz_wl_practice` |
   | Q-T | t,d,internK,g3 | `biz_wl_internship_training` |
   | U-W | j4,r4,g4 | `biz_wl_course_design` |
   | X / Y | g5Liberal **/** g5Scitech | `biz_wl_thesis` 按 discipline_category 分流 |
   | Z-AB | w,r6,g6 | `biz_wl_concentrated_internship` |
   | AC | g7FirstClassroom | `summary.G7`（Task 5 新增） |
   | AD-AE | g8Second,g9Other | `summary.G8/G9` |
   | AF | g9Remark | `summary.G8_remark`（Task 5 新增） |
   | AG | g10Teaching | `summary.G10` |
   | AH | g11Service | `summary.G11` |
   | AI | g11Remark | `summary.G11_remark`（Task 5 新增） |
   | AJ | totalWorkload | `summary.total_workload` |
   | AK | ratedWorkload | `summary.rated_workload` |
   | AL | excessWorkload | `summary.excess_workload` |
   | AM | teacherSign | `summary.teacher_sign` |

2. **行粒度转换**：按「开课任务」聚合 —— 同一 `task_id` 的 G1~G6 明细合为一行；无对应环节的列留空
3. **教师级列只填首行**（A、B、AC~AM），其余行留空——与参考件一致
4. 导出后与参考件逐列比对（列数、列名、合并方式）
5. 提交：`feat(export): 附件1 改为 39 列一行一开课任务（对齐「-新」模板）`

#### Task 18：附件1 固定行与签字区

**Files:**
- Modify: `BizExportController`（附件1 表头/表尾）

**Steps:**
1. 第 1 行标题（A1:AM1 合并）：`潍坊理工学院{学年}学年第{学期}学期{学院}教师工作量统计表（表一）`
2. 第 2 行 12 组分组表头 + 合并单元格；第 3 行列名行；第 4 行 X/Y 两列「文科」「理工科」
3. 表末：`总计：` + 超额工作量合计
4. 表末签字区：`院（部）领导签字：` `单位（盖章）` `年 月 日`（**留空，线下签**）
5. 表末备注三行（班主任/职务变动须标明任职时间；阅读办法；未说明的咨询教务）
6. 提交：`feat(export): 附件1 补齐标题行/分组表头/总计/签字区/备注`

> 建议改用「打开模板填数」而非从零建表——`else/附件1...-新.xlsx` 已是正确格式，POI 打开原文件逐格填值最省事、最能保住合并单元格与样式（B 的 D16 决策同理）。

#### Task 19：附件2 按新模板重做

**Files:**
- Rewrite: `PaySummaryExportDTO.java`
- Modify: `BizExportController`（附件2 组装）
- Create: `rear/.../domain/dto/AllowanceDetailExportDTO.java`（其他酬金明细 15 列）

**Steps:**
1. 主表 10 列：`院、部 | 工号 | 姓名 | 身份证号 | 职称（评）| 超额工作量 | 课时酬金（元）| 其他酬金 | 总金额（整数）| 本人签字`
   - **身份证号依赖 Task 4**；`总金额 = round(超额工作量 × 课时酬金 + 其他酬金)`
2. 其他酬金明细 15 列：`教师姓名 | 单独重修辅导人数 | 重修辅导酬金A | 毕业实习指导人数 | (集中/分散子列) | 指导毕业实习酬金B | 毕业论文（设计）重修指导人数 | 指导论文重修酬金C | 代阅卷人数/课程 | 代阅卷酬金D | 第一课堂讲座名称/时长 | 讲座酬金E | 运动会裁判员酬金F | 游泳馆夜间值班工作量 | 夜间值班酬金G | 其他酬金=A+B+C+D+E+F+G`
   - 来源：`biz_allowance_item`（`student_count` / `duration_hours` / `days` / `class_count` / `workload_units` / `lecture_name` / `amount`）
3. 固定行：`合计` / `教务助理审核签字` / `院（部）领导签字 + 单位盖章 + 年月日`（留空）/ 注 1、注 2
4. 提交：`feat(export): 附件2 对齐「-新」模板（含身份证号与其他酬金明细）`

---

### Phase 5：经费口径回签（不阻塞代码，参数已可配）

#### Task 20：整理回签单并锁定默认值

**Files:**
- Create: `docs/规则确认单-2026-09.md`

**Steps:**
1. 需回签项（按《办法》整理）：

   | 项 | 条文 | 待定内容 | A 现状 |
   |---|---|---|---|
   | 200% 封顶 | 第二十条4 | 540 / 360 / 不封顶 | 540（`CAP_200PCT`） |
   | G4 超 60 人 | 第十四条4 | 截断 / 不截断 | 截断（Task 10 拟改） |
   | 其他酬金 A 项 ≥20 人 | 第十五条1(2) | 归零 / 手工金额 | 手工金额（Task 12 拟改） |
   | 专业负责人 100 学时 | 第十六条注 | 占 / 不占 180 上限 | 未建模 |
   | G5 艺术类 K5 | 第十四条5 | 按文史 / 另定 | 按文史 |

2. **每项配具体金额影响数字**（照 B 的确认单写法，教务处才容易勾选）
3. 回签前建议：**默认值取参考件实际执行过的口径**（如 200% 不封顶），理由是该口径学校实际用过；待回签后改结论值
4. ⚠ `CAP_200PCT` 已是可配参数，**回签结论只改一条数据**，不需要改代码
5. 提交：`docs: 经费口径回签单`

---

### Phase 6：界面收敛

#### Task 21：8 个明细页合并为 1 个「工作量明细」页

**Files:**
- Create: `front/RuoYi-Vue3/src/views/system/workloadDetail/index.vue`
- Delete: `wlTheory/` `wlPractice/` `wlInternshipTraining/` `wlCourseDesign/` `wlThesis/` `wlConcentratedInternship/` `wlManagement/` `workloadItem/`（8 个目录）
- Modify: `front/RuoYi-Vue3/src/api/system/`（保留 API，页面不再各自调用）
- Modify: 菜单 SQL

**Steps:**
1. 新页用「**类别列 + 可折叠环节分组**」呈现 —— 一行一开课任务，展开显示该任务的 G1~G6 因子
2. 保留导出入口与筛选（学期/教师/学院/类别）
3. ⚠ **表结构本期不动**（7 张 `biz_wl_*` 保留）——只合并页面，降低风险
4. 提交：`refactor(ui): 8 个明细页合并为工作量明细页`

#### Task 22：仪表盘收敛为 2 个

**Files:**
- Delete: `AdminDashboard.vue` / `LeaderDashboard.vue`（若 Task 3 未删）
- Keep: `TeacherDashboard.vue` / `JiaoWuDashboard.vue`
- Modify: 路由 + 菜单 SQL

**Steps:** 删除 → 检索残留引用 → `npm run build:prod` → 提交 `chore(ui): 仪表盘收敛为教师/教务处两个`

#### Task 23：菜单与按钮权限同步

**Files:**
- Create: `rear/sql/22_menu_simplify.sql`

**Steps:**
1. 删除被合并页面的菜单与其按钮权限（Task 21 删掉的 8 页对应条目）
2. 删除 leader 仪表盘、审批 `sign` 的权限
3. 保留 role1/2/3 授权（**按简化后的两级角色重新梳理**：teacher=4、教务处=3、admin=1）
4. 脚本幂等（先 `DELETE` 再 `INSERT`），并提供「存量库执行」说明
5. 提交：`chore(sql): 菜单与按钮权限随减量同步`

---

### Phase 7：去 Redis

#### Task 24：规则参数改本地缓存

**Files:**
- Modify: `rear/.../calc/rule/RuleParamServiceImpl.java`
- Modify: `rear/workload-admin/src/main/resources/application.yml`（Redis 配置保留但非必需）
- Modify: `CLAUDE.md` / `PROJECT_CONTEXT.md`

**Steps:**
1. 动机：规则仅 39 条；Redis 缓存**无 TTL** 导致「改库不生效」（A-8），且增加部署组件
2. 改为：进程内 `ConcurrentHashMap` + `@Scheduled(fixedDelay = 60s)` 定时刷新；或启动加载 + 规则维护保存后主动刷新
3. **必须解决原坑**：规则维护 Service 保存后调用 `evict`，否则问题只是从 Redis 换到内存
4. 前端「规则维护」页保存后，提示「已生效」；不再需要手工删键
5. 移除 Redis 依赖后，确认登录态等其它功能不依赖 Redis（**先全文检索 `RedisCache` 使用点**）
   - ⚠ 若 RuoYi 的验证码/会话用了 Redis，**不能整体摘除**，只摘规则缓存这一处
6. 提交：`refactor(rule): 规则参数改本地缓存，移除对 Redis 的强依赖`

---

### Phase 8（**待你勾选，暂不纳入**）：B 的吸收项

`docs/导出对齐与减量实施方案-2026-09-10.md` 第 5.1 节列了 13 项参考价值、第 5.2 节列了 12 项理念分歧。
**你尚未逐项勾选**，因此本计划不含这部分。其中最可能值得纳入的三项：

| 项 | 为什么 | 若不纳入的后果 |
|---|---|---|
| **对账机制**（与真实表逐格 diff） | 目前**没有任何证据**证明 A 算的钱与教务处实发一致 | 上线后第一笔工资错在全校也发现不了 |
| **参数 ↔ SQL 一致性测试** | 39 条规则改错没人拦 | 静默算错钱 |
| **告警码体系** | A 无统一告警，教师看不到「为什么少」 | 解释成本仍是问题 |

---

## 环境坑点（执行提醒）

1. **`mvn` 不在 bash PATH** —— 用 IDEA，或 `C:\...\maven\bin\mvn.cmd` 完整路径；无 `mvnw`
2. **后端已在 8084 运行** —— 每次改代码后**必须重启**才生效；jar 与源码不同步会给出旧行为
3. **Phase 6 之前**：改 `biz_workload_rule` 后**必须手工删 Redis 键** `wl_rule:<CODE>`，否则重启也读旧值
4. **SQL 脚本顺序**：新增脚本编号延续 `16_`~`22_`，全部要求**幂等**（可重复执行）
5. **编译不过时的回退**：Task 1 删模块若报找不到类，说明有隐藏引用，需先排查再删
6. **口径类 Task（8~12、14）都会改变数字** —— 每个都必须**改前记录、改后比对**，不能只看编译通过
7. **前端无 lint 强制** —— 提交前跑 `npm run build:prod` 至少保证能构建

---

## 验证标准（全绿才算完）

| # | 验证 | 方法 | 通过标准 |
|---|---|---|---|
| 1 | 编译 | IDEA `mvn clean package -DskipTests` | BUILD SUCCESS |
| 2 | 前端构建 | `npm run build:prod` | 构建成功无报错 |
| 3 | 单元测试 | `mvn test -pl workload-system` | 2 个既有测试类 + 新增测试全绿 |
| 4 | **基线回归** | 钱伟 `user_id=2003` / `2026-2027-1` | `G7=947.33` `总=982.33` `超额=802.33`；**若非有意变更，数字必须不变** |
| 5 | 审批流 | 前端走 填报→提交→教务处通过 | 终态 `status=2` 且 `lock_time` 非空；无「院领导签字」入口 |
| 6 | 附件1 导出 | 导出后与 `else/附件1...-新.xlsx` 比对 | 39 列、列名一致、合并单元格一致、教师级列只填首行 |
| 7 | 附件2 导出 | 导出后与 `else/附件2...-新.xlsx` 比对 | 10 列主表 + 15 列明细；身份证号为掩码/或按权限解密 |
| 8 | 身份证加密 | 查库 | `id_card_cipher` 为密文，`id_card_mask` 正确；明文不出现在日志 |
| 9 | 无 Redis 依赖 | Phase 6 后停掉 Redis 再启动 | 系统正常运行，规则参数正确加载 |
| 10 | 权限 | 四个测试账号逐页点 | 教师只见本人；教务处可审；无 403/白屏 |

---

## 待确认（阻塞项，开工前请给结论）

| # | 问题 | 影响 Task |
|---|---|---|
| 1 | **批准本计划吗？** 有无需要调整的阶段顺序或范围 | 全部 |
| 2 | **Phase 1 删 3 个模块**是否最终确认？`workload-quartz` 今后做异步重算可能要用 | Task 1 |
| 3 | **G4 改为不截断是「增加」教师工作量**（Task 10），除回签外是否需要先与教务处口头确认？ | Task 10 |
| 4 | **管理服务口径**取「学年值+引擎折半」还是「加 `unit` 字段」？前者要迁移存量数据 | Task 14 |
| 5 | **C1 分组键改动会改变存量结果**（Task 9），是否接受重算后数字变化？ | Task 9 |
| 6 | **Phase 8（B 的吸收项）是否纳入？** 尤其中三项：对账 / 一致性测试 / 告警码 | Phase 8 |
| 7 | 是否需要先**补一批单元测试**再动口径？现在只有 2 个测试类，口径改动缺少防线 | Phase 3 |
| 8 | 附件1 导出用「POI 打开模板填值」还是「从零建表」？我建议前者 | Task 17/18 |
| 9 | 身份证号的**解密导出权限**给到哪一级？导出件带明文身份证是否有合规顾虑？ | Task 4/19 |

---

## 建议的开工顺序

```
Phase 1（瘦身，风险最低）→ Phase 2（补字段）→ Phase 3（口径，需谨慎）
→ Phase 4（导出，依赖前两者）→ Phase 5（回签，并行）
→ Phase 6（界面）→ Phase 7（去 Redis）→ Phase 8（待勾选）
```

**Phase 3 之前建议先做第 7 项待确认（补测试）** —— 口径修正没有测试兜底，改错了只能靠人工发现。
