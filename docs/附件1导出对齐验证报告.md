# 附件1导出对齐验证报告

- 生成方式：`verify_attachment1_export.py`（登录后端 → 调导出端点 → 与模板/数据库逐项比对）
- 验证时间：2026-09-11 20:34
- 检查点总数：841，不一致：0
- 结论：✅ 全部场景零差异：导出忠实性与核算规则一致性均通过

## 场景

| 场景 | 教师 | 学期 | 数据行 | 检查点 | 差异 | 覆盖点 |
|---|---|---|---|---|---|---|
| 2003@2026-2027-1 | 2003 | 2026-2027-1 | 18 | 841 | 0 | 指定场景 |

## 验证覆盖范围

- **结构**：39 列总数；第 2~4 行三层表头逐格全等；表头区与固定块合并单元格集合一致（固定块随行数平移）；标题占位替换（学年/学期号/学院）；总计行/签字区/备注行位置与内容。
- **导出忠实性**：任务行 C~AB 列（26 字段/行）与数据库按任务聚合的落库值逐项比对；教师级列（A/B/AC~AM）只填首行、其余行为空；总计行 AJ。
- **规则一致性**：G4 按 J4×R4×常量、不截断；G6 按 W×min(R6,有效规则上限)×常量；直接核对每条明细落库值，防止数据库与导出同时错误形成假阳性。
- **精度/编码**：数值按 0.005 容差比对（DECIMAL(10,2) 无漂移）；中文字符串全等（utf8mb4 无乱码）；总行数精确等于 4+n+5（无 shiftRows 残留空行）。
- **边界**：多行扩张（n>3，固定块下移）与单行收缩（n<3，固定块上移）均已覆盖。
